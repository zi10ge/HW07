version: '2'
services:
  collector:
    build: collector
    links:
      - web
    volumes:
      - warvol:/home/hw
  
  web:
    build: web
    volumes_from:
      - warvol:/home/hw

volumes:
  warvol: